1、通过执行hw45_sql_operate.py发现添加索引后的查询速度提高，但记录太少，不够明显。通过索引查找记录，而不是遍历整个table，前者类似数组的查找，后者类似单链表，增加索引后，查找效率提高。
2
2.1、在表中记录多，查询操作多，增删改操作少的时候用；
2.2、数据重复且分布平均的字段（只有很少数据值的列）不用；
2.3、text，image等类型不应该建立索引，这些列的数据量大（假如text前10个字符唯一，也可以对text前10个字符建立索引）。